# 校园通知系统 · 使用说明

> 适用对象：使用者 / 部署者
> 阅读前请先确认后端已启动（见第 2 节）。

---

## 1. 系统组成

- **用户端**：`index.html` —— 基于 Vue 3 + Element Plus 的对话式界面，学生/教师登录后收发通知（含 AI 智能提取）。
- 前端仅保留 Vue 3 + Element Plus 版用户端（旧版原生 JS 版已删除）。
- **后台管理端**：`admin.html` —— 管理员查看统计数据、管理用户、查看使用记录和通知。
- **后端服务**：Express 服务，端口 `3001`，前后端通过它通信。

---

## 2. 启动与初始化

### 2.1 首次准备

```powershell
cd c:\Users\shisan\Desktop\agent\backend
npm install                 # 安装依赖（含 bcryptjs、prisma 等）
```

### 2.2 初始化数据库（可选，重置账号数据时执行）

```powershell
cd c:\Users\shisan\Desktop\agent\backend
npm run seed                # 写入默认密码和 3 个管理员账号
```

> ⚠️ 注意：`seed` 会覆盖所有用户的密码为默认密码，正式使用前请谨慎。

### 2.3 启动后端

```powershell
cd c:\Users\shisan\Desktop\agent\backend
npm start                   # 或 npm run dev（nodemon 热重载）
```

看到 `🚀 校园通知后端已启动: http://localhost:3001` 即成功。

### 2.4 打开页面

直接用浏览器打开：
- 用户端：`c:\Users\shisan\Desktop\agent\index.html`
- 后台管理端：`c:\Users\shisan\Desktop\agent\admin.html`

> 页面直连 `http://localhost:3001/api`，请确保后端已启动。

---

## 3. 账号清单

所有账号登录方式均为「账号 + 密码」。

### 3.1 管理员

| 账号 | 姓名 | 级别 | 权限 | 密码 |
|------|------|------|------|------|
| `admin` | 超级管理员 | 一级管理员 | 可查看全部数据（含管理员信息、用户密码） | `admin123` |
| `admin2` | 二级管理员·张 | 二级管理员 | 看不到管理员信息和用户密码 | `admin123` |
| `admin3` | 二级管理员·李 | 二级管理员 | 看不到管理员信息和用户密码 | `admin123` |

### 3.2 教师

| 工号 | 姓名 | 密码 |
|------|------|------|
| `T001` | 张伟 | `123456` |
| `T002` | 李娜 | `123456` |
| `T003` | 王强 | `123456` |

### 3.3 学生（节选常用）

| 学号 | 姓名 | 年级/专业 | 密码 |
|------|------|-----------|------|
| `2025001` | 郑爽 | 大二 · 计算机科学与技术（社团社长） | `123456` |
| `2023001` | 陈晨 | 大四 · 计算机科学与技术（学习委员） | `123456` |
| `2024001` | 孙悦 | 大三 · 计算机科学与技术（学生会会长） | `123456` |
| `2026001` | 蒋涛 | 大一 · 计算机科学与技术（班长） | `123456` |
| 其余学生 | 2023002~2026003 | —— | `123456` |

> 完整清单见 `账号.md`。

---

## 4. 用户端使用（index.html · Vue3 + Element Plus）

### 4.1 界面概览（对话式）

- 采用类 ChatGPT 的对话气泡界面：**右侧蓝色气泡**为你的消息（头像在右），**左侧白色气泡**为 AI 回复与「提取结果卡片」。
- **桌面端**：左侧为侧边栏（新对话、通知中心、历史记录、后台管理入口、当前用户）。
- **移动端**：左上角 **「☰」三条杠** 按钮唤起侧边栏（抽屉）；通知中心抽屉、登录/编辑弹窗均自动适配手机屏幕。
- 底部输入框输入通知原文，回车或点 ➤ 发送；下方提供快捷示例一键填充。

### 4.2 登录

1. 输入「学号/工号」+「密码」（如 `2025001` / `123456`）。
2. 点击「🔑 登录」。
3. 普通用户直接进入对话界面；**管理员账号登录后会自动跳转到后台管理端**（免二次登录）。

### 4.3 AI 智能提取通知

在输入框输入通知原文，例如：

> 明天下午3点，全体大二学生在操场集合，进行体测，请穿运动服。

AI 会自动提取出：受众、时间、地点、备注，并以「提取结果卡片」展示在气泡中，提供 **存草稿 / 修改 / 确认发送** 按钮供你核对后发送；若缺少必填项会提示「信息不全」。

### 4.4 发送通知

- 发送前会校验身份：
  - 学生必须**担任职务**（如社长、会长、班长）才能发送。
  - 学生**不能**发送给教师。
  - 教师发送不受限制。
- 通知会按受众自动生成收件人，且**不会发送给自己**。
- 也可以将通知**存为草稿**，稍后在「通知中心 → 草稿箱」转正式发送。

### 4.5 通知中心

侧边栏「📥 通知中心」（移动端在抽屉内）有三个标签：
- **收件箱**：我收到的通知，可标记已读。
- **已发送**：我发出的通知，可查看「已读 X/Y」进度。
- **草稿箱**：保存的草稿，可转正式发送。

### 4.6 进入后台管理

- 点击用户端侧边栏的 **「🔐 后台管理」** 入口（移动端在侧边栏抽屉内）即可打开后台管理端 `admin.html`。
- 后台管理端顶栏也有 **「🏫 打开用户端」** 返回链接，两端互通。

---

## 5. 后台管理端使用（admin.html）

### 5.1 登录

输入管理员账号 + 密码（如 `admin` / `admin123`），点击「🔓 登录」。

> 管理员在**用户端**登录也会自动跳转到这里，无需二次输入。

### 5.2 四个功能模块

| 模块 | 功能 |
|------|------|
| 📊 总览 | 用户/学生/教师/通知/未读/使用记录等统计卡片 + 最近通知列表 |
| 📋 使用记录 | 查看用户操作日志（登录、发送通知、增删改用户等），可按操作类型筛选、按姓名/学号搜索、清空记录 |
| 👥 用户管理 | 搜索、按身份筛选、新增/编辑/删除用户；可设置密码、职务等 |
| 📢 通知管理 | 查看全部通知，按标题/内容/受众搜索、按状态筛选，可编辑 |

### 5.3 一级管理员 vs 二级管理员

| 操作/信息 | 一级管理员（admin） | 二级管理员（admin2 / admin3） |
|-----------|---------------------|-------------------------------|
| 看到管理员账号 | ✅ 可以 | ❌ 列表不含管理员 |
| 看到用户密码 | ✅ 可以（显示哈希） | ❌ 无密码列 |
| 创建管理员 | ✅ 可以（身份选「管理员」+ 选级别） | ❌ 无「管理员」选项 |
| 编辑/删除管理员 | ✅ 可以 | ❌ 会提示权限不足 |
| 总览统计 | 含管理员计数（多一张「管理员」卡片） | 统计不含管理员 |
| 使用记录 | 全部 | 看不到管理员的记录 |

### 5.4 新增用户示例（一级管理员）

1. 进入「👥 用户管理」→「＋ 新增用户」。
2. 填写学号/工号、姓名。
3. 选择身份：学生 / 教师 / **管理员**（选管理员后可再选一级或二级）。
4. 填写「密码」（必填，登录用）。
5. 保存后该账号即可登录。

### 5.5 修改密码

1. 进入「👥 用户管理」→ 找到用户 →「✏️ 编辑」。
2. 在「密码」框输入新密码（编辑时留空表示不改密码）。
3. 保存。

---

## 6. 常见问题（FAQ）

| 问题 | 原因 / 解决办法 |
|------|-----------------|
| 提示「无法连接后端服务」 | 后端未启动。在 `backend` 目录运行 `npm start`。 |
| 提示「未找到该用户」 | 学号/工号输入有误，或该账号未初始化（先跑 `npm run seed`）。 |
| 提示「该账号未设置密码」 | 该用户 `password` 为空，用管理员在后台编辑并设置密码，或重新 seed。 |
| 提示「密码错误」 | 密码输错；默认学生/教师密码 `123456`，管理员 `admin123`。 |
| 后台提示「未授权，请先登录后台」 | 登录会话过期（服务重启后内存 token 失效），重新登录即可。 |
| 二级管理员看不到某些数据 | 这是设计上的权限限制：二级管理员看不到管理员信息和用户密码。 |
| 修改 schema 后报错 / 需要改数据库 | 先停止后端服务，再在 `backend` 目录执行 `npx prisma migrate dev`，然后重启。 |

---

## 7. 常用命令速查

| 命令（在 `backend` 目录） | 作用 |
|---------------------------|------|
| `npm start` | 启动后端 |
| `npm run dev` | 启动后端（nodemon 热重载） |
| `npm run seed` | 重置/生成账号数据（会覆盖密码） |
| `npx prisma migrate dev` | 根据 schema 变更生成并应用迁移 |
| `npx prisma studio` | 可视化查看数据库（可选） |
