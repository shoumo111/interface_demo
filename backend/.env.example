# 复制此文件为 .env 并填入真实值。
# 注意：.env 已被根目录 .gitignore 排除，不会被提交到仓库。

# SQLite 数据库文件路径（相对 backend/ 目录）
DATABASE_URL="file:./dev.db"

# 智谱 AI（GLM-4-Flash）API Key，仅在服务端使用，勿暴露给前端
ZHIPU_API_KEY="your-zhipu-api-key"

# 后端监听端口
PORT=3001

# （可选）历史遗留变量：当前代码已不再读取，可忽略
# ADMIN_PASSWORD="admin123"
